apiVersion: v1
kind: Pod
metadata:
  name: bad-pod
  namespace: platform
spec:
  automountServiceAccountToken: false
  containers:
    - name: nginx
      image: harbor.io/nginx
      resources:
        requests:
          cpu: "250m"
          memory: "64Mi"
        limits:
          cpu: "500m"
          memory: "128Mi"
